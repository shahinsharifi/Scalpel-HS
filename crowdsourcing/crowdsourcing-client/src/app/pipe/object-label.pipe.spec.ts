import { ObjectLabelPipe } from './object-label.pipe';

describe('ObjectLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
